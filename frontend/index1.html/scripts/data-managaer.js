import { API_BASE_URL, PREVIEW_PAGE_SIZE } from './config.js';

export let allData = [];
export let filteredData = [];
export let previewPage = 1;
export let uniqueValues = {
    topics: new Set(),
    sectors: new Set(),
    regions: new Set(),
    countries: new Set(),
    sources: new Set()
};

export const fetchData = async () => {
    // Implementation from original code
};

export const applyFilters = () => {
    // Implementation from original code
};

export const resetFilters = () => {
    // Implementation from original code
};

export const updateStats = () => {
    // Implementation from original code
};

export const updateDataPreview = () => {
    // Implementation from original code
};

export const populateFilters = () => {
    // Implementation from original code
};